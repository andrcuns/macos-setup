---

- name: Perform upgrade
  homebrew:
    update_homebrew: yes
    upgrade_all: yes

- name: Install common dependencies
  homebrew:
    name: "{{ item }}"
    state: latest
  with_items:
    - npm
    - go
    - python3
